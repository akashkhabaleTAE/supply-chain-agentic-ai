apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway-service
  namespace: supply-chain-ai
  labels:
    app.kubernetes.io/name: gateway-service
    app.kubernetes.io/part-of: supply-chain-agentic-ai
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: gateway-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: gateway-service
        app.kubernetes.io/part-of: supply-chain-agentic-ai
    spec:
      containers:
        - name: gateway-service
          image: supply-chain-agentic-ai/gateway-service:0.1.0
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          envFrom:
            - configMapRef:
                name: supply-chain-config
            - secretRef:
                name: supply-chain-secrets
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: http
            initialDelaySeconds: 35
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            initialDelaySeconds: 70
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: supply-chain-ai
  labels:
    app.kubernetes.io/name: gateway-service
    app.kubernetes.io/part-of: supply-chain-agentic-ai
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: gateway-service
  ports:
    - name: http
      port: 8080
      targetPort: http
