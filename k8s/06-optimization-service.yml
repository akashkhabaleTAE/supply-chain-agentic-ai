apiVersion: apps/v1
kind: Deployment
metadata:
  name: optimization-service
  namespace: supply-chain-ai
  labels:
    app.kubernetes.io/name: optimization-service
    app.kubernetes.io/part-of: supply-chain-agentic-ai
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: optimization-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: optimization-service
        app.kubernetes.io/part-of: supply-chain-agentic-ai
    spec:
      containers:
        - name: optimization-service
          image: supply-chain-agentic-ai/optimization-service:0.1.0
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8084
          envFrom:
            - configMapRef:
                name: supply-chain-config
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            initialDelaySeconds: 60
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: optimization-service
  namespace: supply-chain-ai
  labels:
    app.kubernetes.io/name: optimization-service
    app.kubernetes.io/part-of: supply-chain-agentic-ai
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: optimization-service
  ports:
    - name: http
      port: 8084
      targetPort: http
